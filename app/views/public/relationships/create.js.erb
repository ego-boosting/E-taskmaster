$("#new-follow").html("<%= j(link_to "フォロー外す", user_relationships_path(@user.id), class: "btn btn-outline-danger rounded-pill float mb-3", remote: true, method: :delete ) %>");
$("#followings-count").text(<%= @user.followings.count %>);
$("#followers-count").text(<%= @user.followers.count %>);